#pragma once

#include <cstddef>

namespace AppConfig::Poll {

inline constexpr size_t MIN_OPTIONS = 2;
inline constexpr size_t MAX_OPTIONS = 20;
inline constexpr size_t REQUEST_MAX_OPTIONS = MAX_OPTIONS;

} // namespace AppConfig::Poll
